---
title: "K8S 调度器与Pod调度"
description: "`kube-scheduler`是k8s的核心组件之一，负责整个集群资源的调度功能。"
date: 2022-08-08
tags: ["K8S"]
---
# K8S 调度器与Pod调度

`kube-scheduler`是k8s的核心组件之一，负责整个集群资源的调度功能。

主要作用是根据特定的调度算法和调度策略，将pod调度到合适的Node节点上，是一个独立的二进制成程序，启动之后会一直监听API server，当获取到podspec.nodename为空的pod，就会对这个pod创建一个binding
